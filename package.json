{
  "name": "ttt-assignment-freq-app",
  "version": "1.0.0",
  "description": "Its an assignment, to calculate top N most frequently occuring words in a text file, given by teribly tiny tales.",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "build": "cd frontend && ng build --prod",
    "postinstall": "cd frontend && ng build --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Swati Kanchan",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "cheerio": "^1.0.0-rc.2",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "request": "^2.85.0",
    "request-promise": "^4.2.2"
  }
}
